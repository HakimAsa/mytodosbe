const CONS = {
  INPUT_STRING: 'Your input should be a valid string',
  TODO: 'Todo',
};

const SYMBOLS = {
  CONS: ':',
  FORWARDSLASH: '/',
  UNDERSCORE: '_',
};

module.exports = { CONS, SYMBOLS };
